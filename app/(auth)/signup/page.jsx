import SignupContainer from "@/container/auth/signup";

export const metadata = {
  title: "Sign up | Innap",
  description: "Create an Innap PrioSuite workspace.",
};

export default function SignupPage() {
  return <SignupContainer />;
}
