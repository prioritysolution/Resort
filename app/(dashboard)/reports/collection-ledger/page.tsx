import CollectionRegisterContainer from "@/container/reports/collection-register";

export const metadata = {
  title: "Collection register | Innap",
  description: "Collections by date/month, or for one reservation.",
};

export default function CollectionRegisterPage() {
  return <CollectionRegisterContainer />;
}
